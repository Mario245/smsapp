<%= simple_form_for(@smsmsg) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :phone_no_to %>
    <%= f.input :phone_no_from %>
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
